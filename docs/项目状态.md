# ReproForge 项目状态

> 更新：2026-07-22（预赛截止日）· 队伍 taostronger · 决赛 8.2

## 一句话

**ReproForge — Bug 复现与回归智能体**（6 节点 + 多输入 + 多模态 + 记忆）。main 已全功能。

## main 当前（`e5448ce`，全功能）

| 功能 | 来源 | 状态 |
|---|---|---|
| 核心 4 节点 + 最小复现（核心创新）| Phase1-3 | ✅ |
| 本地 vLLM（spark-71 qwen3.6, util 0.8 + DeepGEMM=0）| Phase4 | ✅ 实测 |
| VL 视觉（远程 step-3.7 多模态看截图）| PR#4 | ✅ 合 |
| RAG 记忆（chromadb + bge-large）| PR#5 | ✅ 合（**bge 本地缺陷，待挪 spark-71**）|
| C 操作采集（Playwright 监听→actions）| PR#6 | ✅ 合 |
| D 视频输入（视频→ASR+LLM提actions+抽帧）| PR#7 | ✅ 合 |
| thinking 剥标签（本地 qwen3.6 输出干净）| 直接 main | ✅ |
| 美化（emerald + 多商品商城 + 步骤式 UI）| 直接 main | ✅ |
| 商城↔ReproForge 跳转（🐞 Report Bug）| 直接 main | ✅ |
| 语音输入（gr.Audio→ASR→口述）| 直接 main | ✅ |
| 记忆库展示页（📚 列表）| 直接 main | ✅（优雅降级，bge 待 spark-71）|

## ✅ 待办已解决（2026-07-22，PR#8 feature/spark71-bge-vl）

1. **RAG bge 部署挪 spark-71** ✅：spark-71 `:8002` bge 服务（FastAPI，CPU，`~/reproforge/.venv`），本地 chromadb 经 `RemoteEmbeddingFunction` 调远程；演示机不再装 torch/sentence-transformers。
2. **VL 模型部署 spark-71** ✅：spark-71 `:8001` vLLM Qwen2.5-VL-7B（util 0.3，JSON `--limit-mm-per-prompt`），`chat_vision` 本地优先 + 远程 step-3.7 fallback。

→ **三服务全本地**（文本 `:8000` util0.45 + VL `:8001` util0.3 + bge `:8002` CPU，峰值显存 ~108GB / 119GB，留 ~11GB）+ 远程 stepfun fallback 加固。端到端验证通过（smoke_vl confidence 1.00 / smoke_rag 命中 / smoke_fallback 兜底 + 85 测试 passed）。详见 spec + `docs/架构与部署.md`「spark-71 三服务部署」。

**实测坑**（已固化到 `deploy/spark71/`）：DeepGEMM=0、VL `--limit-mm-per-prompt` JSON、0.45+0.4 占满→VL 0.3、模型用 modelscope（hf-mirror Xet 超时）、bge 走 .venv、chromadb 1.x EF 要 embed_documents/embed_query/name。

## demo（今天 7.22 截止）

- 服务：demo `:5173` + 隧道 `:8000` + Gradio `:7860`（混合：本地 qwen3.6 + 远程 VL）
- 启动命令：见 `Demo拍摄操作指南.md` 2.2（`MSYS_NO_PATHCONV=1 PROFILE=local LOCAL_BASE_URL/LOCAL_MODEL ...`）
- 录 demo（按 `Demo拍摄操作指南.md`）：Bug1 复现 + VL + Issue
- 剩余提交：录视频 + 合影 + 征文 CSDN/知乎 + 填金数据 `https://mdmv7cyg.jsjform.com/f/eyZ9z5`

## 决赛（8.2）

- bge 挪 spark-71（RAG 真 ingest + 记忆库数据）
- VL 模型 spark-71（Qwen2.5-VL 本地双轨）
- 自动截图（接 capture/reproduction 失败截图）
- C/D 真跑端到端验证（录制/视频）

## 关键状态

- 代码：`github.com/taostronger/reproforge`（main = `e5448ce`，全功能；历史已 filter-repo 抹真名）
- 匿名：**tz**（队长）/ **liwenY**（队员）（memory + CLAUDE.md 固化）
- 双轨：**本地为主 + 远程 fallback**（设 `LOCAL_*` 开本地，无则纯远程=开发态）；文本本地 vLLM qwen3.6 / VL 本地 Qwen2.5-VL / 远程 stepfun 兜底
- 本地服务：demo :5173 / 隧道 :8000 + :8001 + :8002（→spark-71 三服务）/ Gradio :7860
- VL/RAG 开关：`REPROFORGE_VL` / `REPROFORGE_MEMORY`（off 则降级，主流水线照跑）
